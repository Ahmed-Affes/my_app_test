version: "3.9"

services:
  backend:
    build: ./backend-node
    ports:
      - "3010:3001"

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
